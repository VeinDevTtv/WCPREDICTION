{
  "name": "wcprediction-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix web ci && npm --prefix web run build",
    "dev": "npm --prefix web run dev",
    "preview": "npm --prefix web run preview"
  }
}
